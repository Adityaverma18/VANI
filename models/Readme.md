# Models Folder

This folder stores trained model weights for emotion and speaker recognition.

Model files are not uploaded here due to large size limitations.  
Please train the models using `train_emotion.py` and `train_speaker.py`  
or download the pre-trained models from here:

[Google Drive Link] (add your link if available)
